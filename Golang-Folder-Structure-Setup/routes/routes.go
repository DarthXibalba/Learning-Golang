package routes

func Routes() {
	// code for route should be placed here
	// also controller name can be passed here

	//1. Routes called -> Controllerconst
	//2. Controller called -> Serviceconst
	//3. Service does the required things and also calls -> DB operations like fetch/update/etc and returns the result.
}
